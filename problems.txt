why src_dir must not be empty? Do we have a reason for this?
Why does the test cicd install software not through rsconstruct? Why don't we have a subcommand in rsconstruct tools to install all tools needed?
Scan all tests and find tests that could be asserted at compile time.

"rsconstruct tools list"  shows an inconsistent tools list. Two of them are paths relative to current folder and the rest are just names. Make them all names.
